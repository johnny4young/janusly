// Package boot wires configuration, the pgx pool, the migration probe and the
// observability endpoints. Populated in T-001; this file only anchors the
// package so the T-000 smoke test has a home.
package boot
