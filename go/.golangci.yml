version: "2"

linters:
  default: standard
  exclusions:
    rules:
      # The grammar package ports the reference evaluator's error messages
      # verbatim — they are a wire/parity contract, capitalization included.
      - path: internal/grammar/
        linters: [staticcheck]
        text: "ST1005"
