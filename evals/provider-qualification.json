{
  "schemaVersion": "1",
  "datasetVersion": "2026-07-30",
  "provider": "anthropic",
  "model": "claude-haiku-4-5-20251001",
  "generationCases": [
    {
      "id": "billing-approval-en",
      "locale": "en",
      "execution": "browser",
      "parityGroup": "billing-approval",
      "prompt": "Create a workflow for an incoming billing request with customerId and amount. Ask a human to approve the charge, then POST to https://billing.example.com/charges with an Authorization header using {{secret.BILLING_API_TOKEN}}. Never call the billing API before approval.",
      "expect": {
        "requiredTypes": ["approval", "http"],
        "orderedSelectors": [
          [
            { "type": "approval" },
            { "type": "http", "method": "POST" }
          ]
        ],
        "approvalBeforeEveryWrite": true,
        "allowedUrls": ["https://billing.example.com/charges"],
        "requiredFragments": ["{{secret.BILLING_API_TOKEN}}"]
      }
    },
    {
      "id": "billing-approval-es",
      "locale": "es",
      "execution": "api",
      "parityGroup": "billing-approval",
      "prompt": "Crea un flujo para una solicitud de cobro entrante con customerId y amount. Pide aprobación humana, luego haz POST a https://billing.example.com/charges con un encabezado Authorization que use {{secret.BILLING_API_TOKEN}}. Nunca llames la API de cobros antes de la aprobación.",
      "expect": {
        "requiredTypes": ["approval", "http"],
        "orderedSelectors": [
          [
            { "type": "approval" },
            { "type": "http", "method": "POST" }
          ]
        ],
        "approvalBeforeEveryWrite": true,
        "allowedUrls": ["https://billing.example.com/charges"],
        "requiredFragments": ["{{secret.BILLING_API_TOKEN}}"]
      }
    },
    {
      "id": "off-hours-slack-en",
      "locale": "en",
      "execution": "api",
      "prompt": "Create a workflow for an incoming incident. Evaluate whether the current time is outside Monday-Friday 09:00-17:00 Europe/Madrid with the time.window tool, then post to Slack with credential ops_alerts only outside working hours. Declare timeZone, workingHoursStart, and workingHoursEnd as workflow inputs with those defaults and use them in the time.window input.",
      "expect": {
        "requiredTypes": ["tool"],
        "requiredTools": ["time.window", "slack.post"],
        "orderedSelectors": [
          [
            { "tool": "time.window" },
            { "tool": "slack.post" }
          ]
        ],
        "requiredInputs": {
          "timeZone": "Europe/Madrid",
          "workingHoursStart": "09:00",
          "workingHoursEnd": "17:00"
        },
        "requiredFragments": [
          "ops_alerts",
          "{{context.input.timeZone}}",
          "{{context.input.workingHoursStart}}",
          "{{context.input.workingHoursEnd}}",
          "context.",
          ".output.result.inWindow === false"
        ],
        "allowedUrls": []
      }
    },
    {
      "id": "support-review-es",
      "locale": "es",
      "execution": "api",
      "prompt": "Cuando llegue un ticket de soporte, usa IA para redactar una respuesta, permite que una persona la revise y apruebe, y solo después envíala con la herramienta email.send. Nunca envíes el correo antes de la aprobación.",
      "expect": {
        "requiredTypes": ["ai", "tool"],
        "anyOfTypes": ["approval", "human_form"],
        "requiredTools": ["email.send"],
        "orderedSelectors": [
          [
            { "typeAny": ["approval", "human_form"] },
            { "tool": "email.send" }
          ]
        ],
        "approvalBeforeEveryWrite": true,
        "allowedUrls": []
      }
    }
  ],
  "patchCases": [
    {
      "id": "write-approval-en",
      "locale": "en",
      "providerFailure": "webhook",
      "failingNodeId": "charge",
      "workflow": {
        "dslVersion": "1.0",
        "id": "provider-patch-write",
        "name": "Provider patch write",
        "nodes": [
          { "id": "start", "type": "noop", "config": {} },
          {
            "id": "charge",
            "type": "http",
            "config": {
              "url": "http://provider-simulator:4010/webhook/provider-qualification-charge",
              "method": "POST",
              "body": { "customerId": "customer-qualification", "amount": 100 }
            }
          }
        ],
        "edges": [{ "from": "start", "to": "charge" }]
      },
      "expect": {
        "approachLabels": ["add_approval"],
        "approvalBeforeFailingNode": true,
        "structural": true
      }
    },
    {
      "id": "transient-read-es",
      "locale": "es",
      "providerFailure": "webhook",
      "failingNodeId": "fetch",
      "workflow": {
        "dslVersion": "1.0",
        "id": "provider-patch-read",
        "name": "Provider patch read",
        "nodes": [
          { "id": "start", "type": "noop", "config": {} },
          {
            "id": "fetch",
            "type": "http",
            "config": {
              "url": "http://provider-simulator:4010/webhook/provider-qualification-read",
              "method": "GET"
            }
          }
        ],
        "edges": [{ "from": "start", "to": "fetch" }]
      },
      "expect": {
        "approachLabels": ["add_retry", "raise_timeout"],
        "approvalBeforeFailingNode": false,
        "structural": false,
        "rationaleLanguage": "es"
      }
    }
  ],
  "gates": {
    "minimumGenerationFidelityRate": 0.75,
    "minimumPatchPassRate": 1,
    "minimumParityJaccard": 0.5,
    "maximumFallbackRate": 0
  }
}
