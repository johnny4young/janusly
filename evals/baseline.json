{
  "aiModeRate": 0.8,
  "shapePassRate": 0.7,
  "tolerance": 0.1,
  "note": "Conservative seed floors for the AI generation golden suite. Refresh on a keyed local run via `pnpm evals:baseline`. aiModeRate = fraction of requiresMode:ai cases returning mode:ai; shapePassRate = fraction meeting node-shape expectations; tolerance = allowed drop below each floor before it counts as a regression.",
  "updatedAt": null
}
