// GENERATED by scripts/generate-api-guards.mjs from contract/openapi.json.
// Do not edit: run `make generate` after changing the Go manifest.

import type * as Api from "../../api-types.generated"
import { arrayOf, isAny, isBoolean, isFiniteNumber, isShape, isString, recordOf, shape } from "../../guards"
import { isWorkflowNodeConfig } from "./WorkflowNodeConfig"

export function isCanonicalWorkflowDoc(value: unknown): value is Api.CanonicalWorkflowDoc {
  return isShape(value, {
    dslVersion: isString,
    edges: arrayOf(shape({
      from: isString,
      to: isString,
    }, {
      condition: isString,
      id: isString,
      onError: isBoolean,
    })),
    nodes: arrayOf(shape({
      config: isWorkflowNodeConfig,
      id: isString,
      type: isString,
    }, {
      label: isString,
    })),
  }, {
    id: isString,
    inputs: isAny,
    metadata: shape({
      tags: arrayOf(isString),
    }, {
      description: isString,
    }),
    name: isString,
    outputs: recordOf(isString),
    recovery: isAny,
    templatePolicy: isString,
    ui: shape({}, {
      positions: recordOf(shape({
          x: isFiniteNumber,
          y: isFiniteNumber,
        })),
    }),
  })
}
