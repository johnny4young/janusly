# Cloudflare Pages project that owns janusly.app + www.janusly.app (the
# marketing site). The product ships separately as the janusly executable.
name = "janusly-web"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"
